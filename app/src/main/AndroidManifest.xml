<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tachosprojectv2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity
            android:name=".resgiter"
            android:label="@string/title_activity_resgiter"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".MenuAct"
            android:label="@string/title_activity_menu"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".TabRegistro"
            android:label="@string/title_activity_tab_registro"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".RegistroAct2" />
        <activity android:name=".RegistroAct" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>